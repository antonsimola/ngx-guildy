import { GuildyContainerDirective } from './guildy-container.directive';

describe('GuildyContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new GuildyContainerDirective();
    expect(directive).toBeTruthy();
  });
});
