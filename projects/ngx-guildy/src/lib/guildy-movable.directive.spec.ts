import { GuildyMovableDirective } from './guildy-movable.directive';

describe('GuildyMovableDirective', () => {
  it('should create an instance', () => {
    const directive = new GuildyMovableDirective();
    expect(directive).toBeTruthy();
  });
});
